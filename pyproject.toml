[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "mds-project"
version = "0.1.0"
requires-python = ">=3.12"

[tool.setuptools.packages.find]
where = ["."]
include = ["scripts*"]

[tool.pytest.ini_options]
pythonpath = [".", "tests/APIs"]
